export interface TimerInterface {
  workTime: number;
  bWorkTime: number;

  breakTime: number;
  bBreakTime: number;

  minutes: string;
  seconds: string;
  grades: number;

  work: boolean;
  break: boolean;
}
