import FocusIcon from '../icons/FocusIcon';
import style from './Focus.module.scss';

const Focus = () => (
  <span className={style.work__focus}>
    Focus <FocusIcon className={style.icon} />
  </span>
);

export default Focus;
